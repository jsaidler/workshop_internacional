{
  "private": true,
  "scripts": {
    "test:browser": "playwright test tools/browser-tests/form-editor.spec.cjs --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
