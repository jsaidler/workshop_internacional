(()=>{
'use strict';
const params=new URLSearchParams(location.search),pageId=Number(params.get('page')||0),inspector=document.querySelector('#inspector'),frame=document.querySelector('#page-frame');
if(!pageId||!inspector||!frame)return;
let options=null,loading=null;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
async function loadOptions(){if(options)return options;if(loading)return loading;loading=fetch(`/admin/api/cms-access-options.php?page=${pageId}`,{credentials:'same-origin'}).then(async r=>{const data=await r.json();if(!r.ok)throw new Error(data?.error?.code||'access_options_failed');options=data;return data;});return loading;}
function markDirty(){const name=inspector.querySelector('#s-name,#section-name');if(name)name.dispatchEvent(new Event('change',{bubbles:true}));}
function selectedSection(){return frame.contentDocument?.querySelector('.cms-section-selected')||null;}
function setAttr(section,name,value){value=String(value??'').trim();if(value)section.setAttribute(name,value);else section.removeAttribute(name);}
function renderConditional(host,section){const access=host.querySelector('#cms-access-audience')?.value||'public',availability=host.querySelector('#cms-access-availability')?.value||'immediate';host.querySelector('[data-access-cohort]').hidden=access!=='cohort';host.querySelector('[data-access-schedule]').hidden=availability!=='scheduled';host.querySelector('[data-access-lesson]').hidden=availability!=='lesson';
 setAttr(section,'data-cms-access',access==='public'?'':access);setAttr(section,'data-cms-cohort-id',access==='cohort'?host.querySelector('#cms-access-cohort')?.value:'');setAttr(section,'data-cms-availability',availability==='immediate'?'':availability);setAttr(section,'data-cms-visible-from',availability==='scheduled'?host.querySelector('#cms-visible-from')?.value:'');setAttr(section,'data-cms-visible-until',availability==='scheduled'?host.querySelector('#cms-visible-until')?.value:'');setAttr(section,'data-cms-lesson-id',availability==='lesson'?host.querySelector('#cms-access-lesson')?.value:'');}
async function injectSectionControls(){const section=selectedSection();if(!section||inspector.querySelector('[data-cms-access-controls]'))return;const data=await loadOptions();if(!selectedSection()||selectedSection()!==section)return;
 const ds=section.dataset,access=ds.cmsAccess||'public',availability=ds.cmsAvailability||'immediate';
 const block=document.createElement('div');block.className='inspector-section';block.dataset.cmsAccessControls='1';block.innerHTML=`<hr><header><p class="eyebrow">Acesso</p><h2>Visibilidade da seção</h2></header><label>Público<select id="cms-access-audience"><option value="public">Todos os visitantes</option><option value="authenticated">Usuários autenticados</option><option value="activity">Participantes deste curso</option><option value="cohort">Turma específica</option></select></label><label data-access-cohort>Turma<select id="cms-access-cohort"><option value="">Escolha a turma</option>${data.cohorts.map(c=>`<option value="${c.id}">${esc(c.title)}</option>`).join('')}</select></label><label>Disponibilidade<select id="cms-access-availability"><option value="immediate">Imediata</option><option value="scheduled">Agendada</option><option value="lesson">Controlada por aula</option></select></label><div data-access-schedule><label>Liberar em<input id="cms-visible-from" type="datetime-local" value="${esc(ds.cmsVisibleFrom||'')}"></label><label>Encerrar em <span class="inspector-note">(opcional)</span><input id="cms-visible-until" type="datetime-local" value="${esc(ds.cmsVisibleUntil||'')}"></label><p class="inspector-note">Horário: ${esc(data.timezone)}.</p></div><label data-access-lesson>Aula<select id="cms-access-lesson"><option value="">Escolha a aula</option>${data.lessons.map(l=>`<option value="${l.id}">${esc(l.title)}</option>`).join('')}</select></label><p class="inspector-note">A regra pertence à própria seção do CMS. O servidor remove conteúdo sem acesso antes de gerar o HTML.</p>`;
 inspector.append(block);block.querySelector('#cms-access-audience').value=access;block.querySelector('#cms-access-availability').value=availability;block.querySelector('#cms-access-cohort').value=ds.cmsCohortId||'';block.querySelector('#cms-access-lesson').value=ds.cmsLessonId||'';renderConditional(block,section);
 block.querySelectorAll('select,input').forEach(el=>el.addEventListener('change',()=>{renderConditional(block,section);markDirty();}));
}
async function injectPageAccess(){if(!inspector.querySelector('#p-title,#page-title')||inspector.querySelector('[data-cms-page-access]'))return;const data=await loadOptions();if(!inspector.querySelector('#p-title,#page-title'))return;const block=document.createElement('div');block.dataset.cmsPageAccess='1';block.innerHTML=`<hr><label>Acesso à página<select id="cms-page-access"><option value="public">Pública</option><option value="authenticated">Usuários autenticados</option><option value="activity">Participantes deste curso</option></select></label><button type="button" class="panel-button" id="cms-page-access-save">Salvar acesso da página</button><p class="inspector-note">Para restrições parciais, selecione a seção na página e configure o acesso nela.</p>`;inspector.append(block);block.querySelector('#cms-page-access').value=data.pageAccess==='enrolled'?'activity':data.pageAccess;block.querySelector('#cms-page-access-save').onclick=async()=>{const value=block.querySelector('#cms-page-access').value;const r=await fetch('/admin/api/cms-page-access-save.php',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({pageId,access:value})});const body=await r.json().catch(()=>({}));if(!r.ok){alert(body?.error?.message||'Não foi possível salvar o acesso.');return;}options.pageAccess=value;block.querySelector('#cms-page-access-save').textContent='Acesso salvo';setTimeout(()=>block.querySelector('#cms-page-access-save').textContent='Salvar acesso da página',1200);};}
async function refresh(){try{await injectSectionControls();await injectPageAccess();}catch(e){console.error(e)}}
const observer=new MutationObserver(()=>queueMicrotask(refresh));observer.observe(inspector,{childList:true,subtree:true});frame.addEventListener('load',refresh);refresh();
})();
